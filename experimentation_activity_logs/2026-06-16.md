# Experimentation Activity Log: 2026-06-16

## 📝 Activities & Milestones
*   **Dataset Downloader Script:** Created and executed `src/download_datasets.py` to download and prepare the official evaluation datasets.
*   **Datasets Downloaded:** Successfully fetched and prepared:
    *   `data/gsm8k_test.jsonl` (GSM8K math reasoning test set)
    *   `data/svamp.json` (SVAMP math reasoning dataset)
    *   `data/pii_extraction_train.json` (Cleanlab PII extraction dataset)
    *   `data/financial_ner_train.json` (Cleanlab Financial NER extraction dataset)
*   **Baseline Evaluation:** Verified the baseline evaluation script (`src/evaluate.py`) on `llama3.2` (3B, Q4_0) for both `math` and `json` tasks. Both passed with **100.00%** accuracy on the 5-example baseline sets.
*   **Repository Sync:** Set up a `.gitignore` to keep local Python virtual environments and OS metadata files clean.

## 📊 Results & Benchmarks
*   **llama3.2:latest (Q4_0) Baseline Verification:**
    *   **JSON Extraction:** **100.00%** (5/5 correct)
    *   **Math Reasoning (GSM8K Subset):** **100.00%** (5/5 correct)
*   *Observation:* The model successfully handled the short baseline tasks. With the full datasets now downloaded to `data/`, we are prepared to scale up the benchmark evaluations to observe quantization degradation on these larger sets.

## 💻 Code Changes & Commits
*   **Dataset Setup & Workspace Configuration:** Added dataset download utility `src/download_datasets.py`, fetched the benchmark datasets to `data/`, and configured `.gitignore`.
    *   **Commit Hash:** `b60c70f`

